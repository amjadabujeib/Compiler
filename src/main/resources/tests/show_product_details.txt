route "/products/<id>":
    template product_details:
        <html>
            <head>
                <title>{{ product.name }} details</title>
                <style>
                    body { font-family: "Poppins", sans-serif; background: #f5f7fb; padding: 3rem; color: #222; }
                    .detail { max-width: 920px; margin: 0 auto; background: white; border-radius: 24px; box-shadow: 0 24px 48px rgba(0,0,0,0.06); padding: 2rem; display: grid; grid-template-columns: 280px 1fr; gap: 2rem; align-items: start; }
                    .detail img { width: 100%; height: auto; object-fit: contain; border-radius: 12px; }
                    .meta { display: grid; gap: 0.75rem; }
                    .price { color: #c0392b; font-size: 1.8rem; font-weight: 600; }
                    a { color: #1d4ed8; text-decoration: none; }
                </style>
            </head>
            <body>
                <section class="detail">
                    <img src="{{ product.picture }}" alt="{{ product.name }}" />
                    <div class="meta">
                        <h1>{{ product.name }}</h1>
                        <p class="price">${{ product.price }}</p>
                        <p>{{ product.details }}</p>
                        <p><a href="/products">Back to products</a></p>
                    </div>
                </section>
            </body>
        </html>
